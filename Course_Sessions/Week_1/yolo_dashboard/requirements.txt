# YOLO Vision Lab — dashboard dependencies
# Most are already in the `mlcourse` conda env; the last two enable live webcam.

streamlit>=1.36
ultralytics>=8.2
lap>=0.5.12          # linear assignment — required by the object tracker
opencv-python>=4.8
torch>=2.1
plotly>=5.20
pandas>=2.0
numpy>=1.24
Pillow>=10.0

# Optional — only needed for the "Webcam (live)" source.
streamlit-webrtc>=0.47
av>=11.0
